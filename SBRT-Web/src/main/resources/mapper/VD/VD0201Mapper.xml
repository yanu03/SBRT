<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.tracom.bms.domain.VD0201.VD0201Mapper">

	<select id="VD0201G0R0" resultType="Map" parameterType="Map">
		SELECT 			 A.OPER_DT 
				  		, A.REP_ROUT_ID
						, A.VHC_ID
						, A.ROUT_ID
						, A.VHC_NO
						, A.GPS_X
						, A.GPS_Y
						, B.REP_ROUT_NM
						, B.ROUT_NM
						
		
		FROM  			BRT_CUR_OPER_INFO A
		LEFT JOIN   	BMS_ROUT_MST	B
		ON				A.ROUT_ID = B.ROUTE_ID
		WHERE			A.OPER_DT = DATE_FORMAT(NOW(), '%Y/%m/%d')
			<if test="CONTENT1 != null">
				AND A.REP_ROUT_ID = #{CONTENT1}
			</if>
			<if test="CONTENT2 != null and CONTENT2 != ''">
				AND	A.VHC_NO LIKE CONCAT('%', #{CONTENT2}, '%')
			</if>
		ORDER BY		A.REP_ROUT_ID, A.ROUT_ID
	</select>
	
	<select id="VD0201G1R0" resultType="Map" parameterType="Map">
		SELECT 			A.VHC_ID
						,A.DVC_ID
						,A.ORG_ID
						,A.DVC_KIND
						,A.INST_LOC
						,A.DVC_IP
						,A.MNG_ID	
						,A.DVC_TYPE
						,A.DEV_SERIAL_NO
						,A.MODEL_NM
						,A.USE_YN						
						,A.REMARK
						,B.ORG_NM
						,C.DVC_COND
						
		FROM			BMS_DVC_INFO A	
			LEFT JOIN	  	BMS_ORG_MST	B
			ON				A.ORG_ID = B.ORG_ID
			LEFT JOIN	  	BMS_DVC_COND_INFO C
			ON				A.MNG_ID = C.MNG_ID	
		
		WHERE			A.VHC_ID = #{VHC_ID}
	</select>
	
	<select id="VD0201SHI0" resultType="Map">
		SELECT			A.REP_ROUT_ID
						,B.REP_ROUT_NM	
		FROM			BRT_CUR_OPER_INFO A
			LEFT JOIN		BMS_REP_ROUT_MST B
			ON				A.REP_ROUT_ID = B.REP_ROUT_ID
		GROUP BY 		REP_ROUT_ID		
	</select>		
</mapper>